# Скопируйте этот файл в .env (для локального теста) и/или добавьте
# переменную BOT_TOKEN в настройках проекта на Vercel (Settings → Environment Variables).
# НИКОГДА не коммитьте .env с реальным токеном в GitHub.

BOT_TOKEN=вставьте_сюда_новый_токен_бота
